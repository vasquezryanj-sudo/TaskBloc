import Image from 'next/image'
import Link from 'next/link'

export default function NotFound() {
  return (
    <div className="min-h-screen bg-fog-200 flex flex-col items-center justify-center px-6 relative overflow-hidden">

      {/* Background glows */}
      <div className="absolute inset-0 pointer-events-none" aria-hidden="true">
        <div
          className="absolute top-0 left-1/2 -translate-x-1/2 w-[900px] h-[520px] opacity-50"
          style={{ background: 'radial-gradient(ellipse at center top, #d5e0ed 0%, transparent 70%)' }}
        />
        <div
          className="absolute bottom-0 right-0 w-[500px] h-[400px] opacity-30"
          style={{ background: 'radial-gradient(ellipse at bottom right, #fae3c0 0%, transparent 65%)' }}
        />
      </div>

      {/* Main content */}
      <div className="w-full max-w-[360px] relative flex flex-col items-center text-center">

        {/* Logo + wordmark */}
        <div className="w-16 h-16 rounded-[18px] shadow-lg overflow-hidden mb-5 ring-1 ring-navy-200/60">
          <Image
            src="/apple-touch-icon.png"
            alt="MindWell"
            width={64}
            height={64}
            className="w-full h-full object-cover"
            priority
          />
        </div>

        <p
          className="text-base font-bold tracking-tight mb-8"
          style={{ color: '#191970' }}
        >
          MindWell
        </p>

        {/* Card */}
        <div className="w-full bg-white rounded-3xl border border-fog-300 shadow-xl overflow-hidden">
          <div className="h-[3px] bg-gradient-to-r from-ember-300 via-ember-400 to-ember-300" />

          <div className="px-8 py-10 flex flex-col items-center gap-4">
            <p className="text-[6rem] font-bold leading-none tracking-tight" style={{ color: '#191970' }}>
              404
            </p>

            <p className="text-sm text-fog-500 leading-relaxed">
              This page doesn&rsquo;t exist or was moved.
            </p>

            <Link
              href="/home"
              className="mt-2 w-full flex items-center justify-center px-5 py-[13px] rounded-xl text-sm font-semibold text-white transition-all"
              style={{
                background: '#1a3a6b',
                boxShadow: '0 1px 3px rgba(26,58,107,0.25), 0 4px 12px rgba(26,58,107,0.15)',
              }}
            >
              Back to home
            </Link>
          </div>
        </div>

      </div>
    </div>
  )
}
