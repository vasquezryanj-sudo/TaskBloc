'use client'

import { useState } from 'react'
import { format } from 'date-fns'
import Modal from './Modal'

interface Props {
  onClose: () => void
  onSuccess: () => void
}

export default function AddPriorityModal({ onClose, onSuccess }: Props) {
  const today = format(new Date(), 'yyyy-MM-dd')
  const [form, setForm] = useState({
    title: '',
    description: '',
    sessions_needed: '1',
    scheduled_date: today,
  })
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setError('')
    setLoading(true)

    const res = await fetch('/api/priority-items', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        title: form.title.trim(),
        description: form.description.trim() || null,
        sessions_needed: Number(form.sessions_needed),
        scheduled_date: form.scheduled_date,
      }),
    })

    if (!res.ok) {
      const data = await res.json()
      setError(data.error ?? 'Failed to add item')
      setLoading(false)
      return
    }

    onSuccess()
    onClose()
  }

  return (
    <Modal title="Add Priority Item" onClose={onClose}>
      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <label className="block text-sm font-medium text-well-200 mb-1">Title</label>
          <input
            type="text"
            required
            value={form.title}
            onChange={e => setForm(f => ({ ...f, title: e.target.value }))}
            className="w-full bg-well-800 border-0 rounded-lg px-3 py-2.5 text-sm text-well-50 placeholder:text-well-500 focus:outline-none focus:ring-2 focus:ring-amber-500/20 focus:border-amber-500/40 transition-all"
            placeholder="Article title, event, etc."
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-well-200 mb-1">Description (optional)</label>
          <input
            type="text"
            value={form.description}
            onChange={e => setForm(f => ({ ...f, description: e.target.value }))}
            className="w-full bg-well-800 border-0 rounded-lg px-3 py-2.5 text-sm text-well-50 placeholder:text-well-500 focus:outline-none focus:ring-2 focus:ring-amber-500/20 focus:border-amber-500/40 transition-all"
            placeholder="URL, notes, etc."
          />
        </div>

        <div className="grid grid-cols-2 gap-3">
          <div>
            <label className="block text-sm font-medium text-well-200 mb-1">Scheduled date</label>
            <input
              type="date"
              required
              value={form.scheduled_date}
              onChange={e => setForm(f => ({ ...f, scheduled_date: e.target.value }))}
              className="w-full bg-well-800 border-0 rounded-lg px-3 py-2.5 text-sm text-well-50 focus:outline-none focus:ring-2 focus:ring-amber-500/20 focus:border-amber-500/40 transition-all"
            />
          </div>
          <div>
            <label className="block text-sm font-medium text-well-200 mb-1">Sessions displaced</label>
            <input
              type="number"
              min={1}
              max={10}
              value={form.sessions_needed}
              onChange={e => setForm(f => ({ ...f, sessions_needed: e.target.value }))}
              className="w-full bg-well-800 border-0 rounded-lg px-3 py-2.5 text-sm text-well-50 focus:outline-none focus:ring-2 focus:ring-amber-500/20 focus:border-amber-500/40 transition-all"
            />
          </div>
        </div>

        {error && <p className="text-sm text-rose-400">{error}</p>}

        <div className="flex gap-3 pt-1">
          <button
            type="button"
            onClick={onClose}
            className="flex-1 bg-well-800 hover:bg-well-700 text-well-300 hover:text-well-100 px-4 py-2 rounded-full text-sm transition-all"
          >
            Cancel
          </button>
          <button
            type="submit"
            disabled={loading}
            className="flex-1 bg-amber-600 hover:bg-amber-500 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors disabled:opacity-50"
          >
            {loading ? 'Adding…' : 'Add Priority'}
          </button>
        </div>
      </form>
    </Modal>
  )
}
