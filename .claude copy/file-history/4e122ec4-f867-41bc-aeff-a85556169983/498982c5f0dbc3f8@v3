'use client'

import type { PriorityItem } from '@/types'

interface Props {
  item: PriorityItem
  onComplete: () => void
  onDelete: () => void
}

export default function PriorityCard({ item, onComplete, onDelete }: Props) {
  return (
    <div className="bg-well-900 rounded-xl px-4 py-3 flex items-center justify-between gap-4 hover:bg-well-800 transition-colors group">
      <div className="min-w-0">
        <div className="flex items-center gap-2">
          <span className="text-[10px] font-semibold uppercase tracking-widest text-amber-400 bg-amber-500/10 border border-amber-500/20 px-2 py-0.5 rounded-full shrink-0">
            Priority
          </span>
          <p className="font-medium text-well-100 truncate">{item.title}</p>
        </div>
        {item.description && (
          <p className="text-sm text-well-500 mt-0.5 truncate">{item.description}</p>
        )}
        <p className="text-xs text-well-600 mt-0.5">
          {item.sessions_needed} session{item.sessions_needed !== 1 ? 's' : ''} displaced
        </p>
      </div>
      <div className="flex items-center gap-2 shrink-0">
        <button
          onClick={onDelete}
          className="text-xs text-well-500 hover:text-rose-400 px-2.5 py-1 rounded-md bg-well-800 group-hover:bg-well-700 hover:bg-rose-900/40 transition-all"
        >
          Delete
        </button>
        <button
          onClick={onComplete}
          className="w-7 h-7 flex items-center justify-center rounded-full bg-amber-500 hover:bg-amber-400 text-black transition-all"
          title="Mark done"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" className="w-4 h-4">
            <path fillRule="evenodd" d="M16.704 4.153a.75.75 0 0 1 .143 1.052l-8 10.5a.75.75 0 0 1-1.127.075l-4.5-4.5a.75.75 0 0 1 1.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 0 1 1.05-.143Z" clipRule="evenodd" />
          </svg>
        </button>
      </div>
    </div>
  )
}
