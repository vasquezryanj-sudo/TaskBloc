'use client'

import type { PriorityItem } from '@/types'

interface Props {
  item: PriorityItem
  onComplete: () => void
  onDelete: () => void
}

export default function PriorityCard({ item, onComplete, onDelete }: Props) {
  return (
    <div className="bg-well-900 rounded-lg border border-well-800 flex overflow-hidden">
      {/* Amber left accent */}
      <div className="w-1 shrink-0 bg-amber-500" />
      <div className="flex-1 px-4 py-3 flex items-center justify-between gap-4">
        <div className="min-w-0">
          <div className="flex items-center gap-2">
            <span className="text-xs font-semibold uppercase tracking-wide text-amber-400 bg-amber-500/10 border border-amber-500/20 px-1.5 py-0.5 rounded">
              Priority
            </span>
            <p className="font-medium text-well-100 truncate">{item.title}</p>
          </div>
          {item.description && (
            <p className="text-sm text-well-400 mt-0.5 truncate">{item.description}</p>
          )}
          <p className="text-sm text-well-500 mt-0.5">
            {item.sessions_needed} session{item.sessions_needed !== 1 ? 's' : ''} displaced
          </p>
        </div>
        <div className="flex items-center gap-2 shrink-0">
          <button
            onClick={onDelete}
            className="text-sm text-well-400 hover:text-rose-400 px-2 py-1 rounded hover:bg-rose-500/10 transition-colors"
          >
            Delete
          </button>
          <button
            onClick={onComplete}
            className="w-7 h-7 flex items-center justify-center rounded-full bg-amber-500 hover:bg-amber-400 text-white transition-colors"
            title="Mark done"
          >
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" className="w-4 h-4">
              <path fillRule="evenodd" d="M16.704 4.153a.75.75 0 0 1 .143 1.052l-8 10.5a.75.75 0 0 1-1.127.075l-4.5-4.5a.75.75 0 0 1 1.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 0 1 1.05-.143Z" clipRule="evenodd" />
            </svg>
          </button>
        </div>
      </div>
    </div>
  )
}
