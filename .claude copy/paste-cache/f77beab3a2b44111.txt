Please make the following changes to index.html. If the changes are too large to make in one pass, split them into sequential steps and complete each one before moving to the next.
1. Tags system

Add a tags field to each task (array of {label, color})
In the task modal, add a tag builder: a text input + color picker (at least 8 color swatches to choose from) + an "Add" button. Tags should display as removable chips inside the modal.
Render tags as small colored chips on the collapsed task bubble, below the title

2. Due & Start dates

Add due and start text fields to the task modal (plain text, e.g. "Mar 15" or "next Monday")
Show them on the collapsed task bubble as small metadata labels, e.g. Start: Mar 10 and Due: Mar 15

3. Collapsed bubble layout

Line 1: task title + time estimate tag (right-aligned)
Line 2: tags chips + start date + due date, all inline, only shown if values exist

4. Next Week section

Add a next_week Supabase table (same schema as forward_tasks, plus tags, due, start, estimate fields)
Add a nextWeek state array in the App component
Render a "Next Week" card section between the Coming Up strip and the Looking Forward section — same visual style as Looking Forward but with a navy/indigo accent bar instead of purple
Tasks in Next Week should be addable, completable, deletable, draggable to reorder, and openable in a modal
Tasks should be draggable from any day card into Next Week (same pattern as Looking Forward)

5. DB fields

All task objects (daily tasks, forward, next week) should include tags, due, start fields
Include these in all Supabase upsert and load functions

At the end, output the Supabase SQL migrations needed for the new fields and table.